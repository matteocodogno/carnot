services:
  postgres:
    image: 'postgres:latest'
    environment:
      - POSTGRES_DB=arco2
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
    ports:
      - '5436:5432'
